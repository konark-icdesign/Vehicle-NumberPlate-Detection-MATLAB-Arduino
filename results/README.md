# Results folder

When `matlab/main.m` is run, this folder receives:

- `annotated_output.avi` - processed video with bounding boxes
- `detection_log.csv` - per-frame vehicle/plate detections

The repository also includes synthetic reference validation data created while rebuilding the project. It is clearly labelled as a reconstruction/reference test and is not claimed as output from the lost original college source code.
